version: 2
workflows:
  version: 2
  test:
    jobs:
      - test:
          filters:
            tags:
              only: /.*/
jobs:
  test:
    docker:
      - image: circleci/node:17
      - image: darthjee/lemonade:latest
        name: sample
    steps:
      - checkout
      - run:
          name: Set folder
          command: cp source/* ./ -r; rm source -rf
      - run:
          name: NPM install
          command: npm install
      - run:
          name: Debug
          command: ls
      - run:
          name: Jasmine
          command: npx jasmine
